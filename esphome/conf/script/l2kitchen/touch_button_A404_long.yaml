####################################################################
# SCRIPTS

#script:
# on_multi_click action of TB A4 (long click)
- id: tb_04_click_long
  mode: restart
  then:
    # - logger.log: "script tb_04_click_long action."
    - if:
        condition:
          # execute this script only if we are offline
          # lambda: 'return (!id(isDeviceConnected);'

          # execute only if the lightbar is on
          # lambda: 'return (!id(isLightBarOn));'

          # execute this script anyway (TM) for debugging purposes
          lambda: "return (true);"
        then:
          # - logger.log: "script tb_04_click_long action."

          - homeassistant.action:
              action: script.zb_plug_toggle_by_esphome
              data:
                zb_plug_id: "switch.zb_plug_l2_kitchen_microwave"
          - homeassistant.action:
              action: script.zb_plug_toggle_by_esphome
              data:
                zb_plug_id: "switch.zb_plug_l2_kitchen_kettle"
          - homeassistant.action:
              action: script.zb_plug_toggle_by_esphome
              data:
                zb_plug_id: "switch.zb_plug_l2_kitchen_coffeemachine"

          # - logger.log: "script tb_04_click_long action."
####################################################################
# EOF
####################################################################
