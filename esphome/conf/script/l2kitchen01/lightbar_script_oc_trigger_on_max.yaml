####################################################################
# SCRIPTS

#script:
  # triggering the opto coupler in order to switch the light bar off
  - id: lightbar_script_oc_trigger_on_max
    mode: single
    then:
      - logger.log: "The lightbar is now theoretically switched on to max brightness."
      - logger.log: "But as there is no action in the script lightbar_script_oc_trigger_on_max ..."

####################################################################
# EOF
####################################################################
