####################################################################
# SCRIPTS

#script:
  # on_press action of PIR #01 (no matter whether the device is online or offline)
  # just dummy action for testing
  - id: ssr_script_pir_01
    mode: restart
    then:
      # ESP_LOGD("binary_sensor", "l2kitchen01: PIR #01 detected motion, switching SSR01 on.");
      - delay: !secret DELAY_02MIN
      # ESP_LOGD("binary_sensor", "l2kitchen01: switching SSR01 off.");

####################################################################
# EOF
####################################################################
