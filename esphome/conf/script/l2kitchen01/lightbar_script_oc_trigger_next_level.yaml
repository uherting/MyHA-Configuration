####################################################################
# SCRIPTS

#script:
  # triggering the opto coupler in order to switch the light bar off
  - id: lightbar_script_oc_trigger_next_level
    mode: single
    then:
      - logger.log: "The lightbar is now theoretically to the next level."
      - logger.log: "But as there is no action in the script lightbar_script_oc_trigger_next_level ..."

####################################################################
# EOF
####################################################################
