####################################################################
# GLOBAL VARIABLES

#globals:
  - id: isLightBarOnViaBtn
    type: bool
    restore_value: no
    # assume the best, test for the worst later...
    initial_value: 'false'

  - id: isLightBarOnViaPIR
    type: bool
    restore_value: no
    # assume the best, test for the worst later...
    initial_value: 'false'

  - id: isLightBarOn
    type: bool
    restore_value: no
    # assume the best, test for the worst later...
    initial_value: 'false'

####################################################################
# EOF
####################################################################
