####################################################################
# ESP32C3 - onboard LED

# output:
  - platform: gpio
    pin: GPIO8
    id: $esp32c3_onboard_led_id
    # 20260902:
    #   "inverted: false" works for l2lounge09 fine, does not work for l2corridor02, where the LED is always on.
    #   "inverted: true" works for l2corridor02 makes the LED work as expected
    #   I guess the onboard LED of l2lounge09 is connected in a different way than
    #   the one of l2corridor02. For now I will set "inverted: false" and see 
    #   how it goes with the other microcontrollers.
    inverted: true

####################################################################
# EOF
####################################################################
