####################################################################
# OUTPUT

#
# attention:
#   output components are not switches and will not(!!!) show up in Home Assistant.
#

#output:

#############################
# THERMOSTAT_DE

# Thermostat Rotary encoder #2
#   (output), to be connected to thermostat via Opto Couplers
  - platform: gpio
    id: ${thermostat_de01_re2_id}
    pin:
      mcp23017: ${mcp23017_hub01_id}
      number: !secret MCP23017_PIN_B7
      # mode can be INPUT, INPUT_PULLUP or OUTPUT
      mode: OUTPUT
      inverted: false
      # inverted: true

# ############################################################
#  END OF FILE
# ############################################################
