####################################################################
# I2C
####################################################################

  id: !secret I2C_BUS_A
  scl: !secret I2C_SCL_PIN_V3
  sda: !secret I2C_SDA_PIN_V3
  scan: true

  # default values
  # see: https://esphome.io/components/i2c.html#configuration-variables
  # frequency: Set the frequency the I²C bus should operate on. Defaults to 50kHz. Values are 50kHz, 100kHz, 200kHz, … 800kHz

# ############################################################
#  END OF FILE
# ############################################################

