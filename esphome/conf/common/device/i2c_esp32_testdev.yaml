####################################################################
# I2C (esp32 test microcontroller only)
####################################################################

# usage in esp32 test microcontroller, not to be used in anything else
# for details see https://esphome.io/components/i2c.html
  scl: !secret I2C_SCL_PIN_ESP32_TESTDEV
  sda: !secret I2C_SDA_PIN_ESP32_TESTDEV

  # default values
  # see: https://esphome.io/components/i2c.html#configuration-variables
  # frequency: Set the frequency the I²C bus should operate on. Defaults to 50kHz. Values are 50kHz, 100kHz, 200kHz, … 800kHz

# ############################################################
#  END OF FILE
# ############################################################

