####################################################################
# DISPLAY
# configuration for SPI bus, for details see https://esphome.io/components/spi/
# spi:
  mosi_pin: GPIO13 # MOSI
  miso_pin: GPIO12 # MISO (optional, not needed for displays)
  clk_pin: GPIO14  # SCK
  # id: spi_bus_1  # optional, useful if you have multiple SPI buses
