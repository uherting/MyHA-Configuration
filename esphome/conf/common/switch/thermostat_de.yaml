  # Thermostat Boost Button
  - <<: !include conf/common/switch/thermostat_de01.yaml
  - <<: !include conf/common/switch/thermostat_de02.yaml
  # Thermostat Rotary encoder via Opto Couplers
  - <<: !include conf/common/switch/thermostat_de03.yaml
####################################################################
# SWITCHES

# switch:

#############################
# THERMOSTAT_DE
# Thermostat Boost Button
  - platform: gpio
    name: ${thermostat_de01_bb_name}
    id: ${thermostat_de01_bb_id}
    pin:
      mcp23017: ${mcp23017_hub01_id}
      number: !secret MCP23017_PIN_B5
      # mode can be INPUT, INPUT_PULLUP or OUTPUT
      mode: OUTPUT
      inverted: false
      # inverted: true
    restore_mode: ALWAYS_OFF
    # for AppDaemon
    icon: mdi:lamp

#############################
# THERMOSTAT_DE
# Thermostat Rotary encoder via Opto Couplers
  - platform: gpio
    name: ${thermostat_de01_re1_name}
    id: ${thermostat_de01_re1_id}
    pin:
      mcp23017: ${mcp23017_hub01_id}
      number: !secret MCP23017_PIN_B6
      # mode can be INPUT, INPUT_PULLUP or OUTPUT
      mode: OUTPUT
      inverted: false
      # inverted: true
    restore_mode: ALWAYS_OFF
    # for AppDaemon
    icon: mdi:lamp

#############################
# THERMOSTAT_DE
# Thermostat Rotary encoder via Opto Couplers
  - platform: gpio
    name: ${thermostat_de01_re2_name}
    id: ${thermostat_de01_re2_id}
    pin:
      mcp23017: ${mcp23017_hub01_id}
      number: !secret MCP23017_PIN_B6
      # mode can be INPUT, INPUT_PULLUP or OUTPUT
      mode: OUTPUT
      inverted: false
      # inverted: true
    restore_mode: ALWAYS_OFF
    # for AppDaemon
    icon: mdi:lamp

# ############################################################
#  END OF FILE
# ############################################################
