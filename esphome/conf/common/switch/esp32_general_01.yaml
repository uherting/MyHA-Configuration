####################################################################
# SWITCHES

#switch:
  - platform: gpio
    name: ${ESP_ONBOARD_LED_PIN_NAME}
    id:  ${ESP_ONBOARD_LED_PIN_ID}
    pin: ${ESP_ONBOARD_LED_PIN_NUMBER}
    inverted: True

####################################################################
# EOF
####################################################################
