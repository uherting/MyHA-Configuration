####################################################################
# SCRIPTS

#script:
  # triggering the opto coupler in order to switch the light bar off
  - id: lightbar_script_oc_trigger_onoff
    mode: single
    then:
        - 

####################################################################
# EOF
####################################################################
