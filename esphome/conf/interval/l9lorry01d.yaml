####################################################################
# DISPLAY INTERVAL

# show the next page every x seconds, and update the display
  - interval: 3s
    then:
      - lvgl.page.next:
      # the nest cannot be validated.
      # - component.update: ${display_id}

# ############################################################
#  END OF FILE
# ############################################################
