####################################################################
# INTERVAL

  - interval: 1min
    then:
      - globals.set:
          id: isWifiOn
          value: id(${wifi_status_id}).state

# just debugging:
#      - lambda: |-
#          id(${binary_template_sensor_01_id}).publish_state(id(${wifi_status_id}).state);

####################################################################
# EOF
####################################################################
