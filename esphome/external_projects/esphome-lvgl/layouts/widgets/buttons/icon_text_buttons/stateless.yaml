# required vars:
# - uid
# - width
# - height
# - icon

# button:
  id: ${uid}_widget
  width: ${width}
  height: ${height}
  widgets:
    - label:
        id: ${uid}_widget_icon
        text: ${icon}
        text_font: mdi_md
        align: TOP_LEFT
    - label:
        id: ${uid}_widget_text
        text: ${text}
        align: BOTTOM_LEFT
