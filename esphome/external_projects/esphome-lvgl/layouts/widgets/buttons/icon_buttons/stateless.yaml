# required vars:
# - uid
# - width
# - height
# - icon

# button:
  id: ${uid}_widget
  width: ${width}
  height: ${height}
  widgets:
    - label:
        id: ${uid}_widget_icon
        text: ${icon}
        text_font: mdi_lg
        align: TOP_LEFT
