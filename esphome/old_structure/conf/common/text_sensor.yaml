####################################################################
# TEXT_SENSORS
####################################################################

# publish the ESPHome version
  - platform: version
    name: ${friendly_name} ESPHome Version

  - platform: wifi_info
    ip_address:
      name: ${wifi_connection_ip_address_name}
    ssid:
      name: ${wifi_connection_ssid_name}
    bssid:
      name: ${wifi_connection_bssid_name}

# ############################################################
#  END OF FILE
# ############################################################

