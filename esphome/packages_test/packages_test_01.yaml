# packages test #1


substitutions:
  node_name: mydevice
  device_verbose_name: "My Device"
  wifi_uptime_name: "uptime"
  log_level: !secret LOG_LEVEL_INFO

packages:
  wifi: !include packages_test_01/wifi.yaml
  device_base: !include packages_test_01/device_base.yaml

api:
  password: !secret HA_API_PW

sensor:
  - platform: uptime
    name: ${wifi_uptime_name}
