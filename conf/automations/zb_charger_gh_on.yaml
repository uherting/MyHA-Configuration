# ############################################################
#  Turn the ZB plug on GH
# ############################################################
- alias: Turn the ZB plug GH on
  id: "019fc1a7-7d2a-73ea-b778-b0355f8ec6e0"
  description: "This automation turns on the ZB plug GH"
  mode: single
  initial_state: true
  # --------------------- TRIGGER ---------------------
  triggers:
    - trigger: template
      id: "delayed_charging_on"
      value_template: "{{ (is_state('binary_sensor.zb_charger_gh_delayed_charging_on_trigger', 'on')) }}"
      # delayed charging takes place only once per time frame. for details see definition
      # of binary_sensor.zb_charger_gh_delayed_charging_on_trigger .
    - trigger: template
      id: "low_percentage_on"
      value_template: "{{ (is_state('binary_sensor.zb_charger_gh_low_percentage_on_trigger', 'on')) }}"
  # -------------------- CONDITIONS -------------------
  # none whatsoever
  condition: []
  # --------------------- ACTIONS ---------------------
  action:
    - choose:
        - conditions:
            - condition: trigger
              id: "delayed_charging_on"
          sequence:
            - action: switch.turn_on
              entity_id: switch.zb_charger_gh
            - action: input_select.select_option
              target:
                entity_id: input_select.zb_charger_gh_state_current
              data:
                option: "{{ states('input_text.zb_charger_state_current_delayed_charging_in_progress') }}"
        - conditions:
            - condition: trigger
              id: "low_percentage_on"
          sequence:
            - action: switch.turn_on
              entity_id: switch.zb_charger_gh
            - action: input_select.select_option
              target:
                entity_id: input_select.zb_charger_gh_state_current
              data:
                option: "{{ states('input_text.zb_charger_state_current_charging_in_progress') }}"
      default:
        - action: system_log.write
          data:
            message: "ZB plug GH on default action which is not defined @ {{ (now().timestamp()) | timestamp_custom('%Y%m%d%H%M%S') }}"
            level: "error"
            logger: "system_log.external"
# ############################################################
#  END OF FILE
# ############################################################
