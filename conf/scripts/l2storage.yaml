# ############################################################
#
#  Customised scripts for L2Storage01
#
# ############################################################

# --------------------------------------------------------------
# This script toggles an automation in order to simulate spy mode
# --------------------------------------------------------------
l2_storage01_bin_templ_spymode_control:
  alias: Toggle an automation in order to simulate spy mode in L2Storage01
  icon: mdi:text
  description: This script toggles an automation in order to simulate spy mode
  mode: queued
  sequence:
    - service: automation.toggle
      data: {}
      target:
        entity_id: automation.l2_storage_motion_detected_ssr01_on
    # - service: binary_sensor.template.publish
    #   data:
    #   target:
    #     entity_id:
    #       binary_sensor.l2_storage01_bin_templ_spymode
    #       # id: binary_sensor.l2_storage01_bin_templ_spymode
    #       # state: !lambda |-
    #       #   if (id(binary_sensor.l2_storage01_bin_templ_spymode).state) {
    #       #     return false;
    #       #   } else {
    #       #     return true;
    #       #   }

    # - service: input_boolean.toggle
    #   entity_id: input_boolean.l2storage_spymode
# ############################################################
#  END OF FILE
# ############################################################
