####################################################################
####################################################################
#
# testing tab --- lovelace configuration
#
####################################################################
####################################################################

icon: mdi:test-tube
title: Testing Only
path: testing
badges: [ 
          person.uwe, 
          person.gillian,
          sun.sun,
          binary_sensor.workday_sensor,
          binary_sensor.updater
  ]

cards:
  - type: entities
    title: Precipitation (OpenWeatherMap)
    show_header_toggle: false
    entities:
      - sensor.openweathermap_forecast_precipitation
      - sensor.openweathermap_forecast_precipitation_probability

      # OpenWeatherMap
  - type: weather-forecast
    entity: weather.openweathermap

  # Norwegian Meteorological Institute
  - type: weather-forecast
    entity: weather.home_hourly

  # # AccuWeather
  # - type: weather-forecast
  #   entity: weather.home431

  # # Deutscher Wetterdienst (DWD)
  # - type: weather-forecast
  #   entity: weather.dwd_weather_h431

####################################################################
# EOF
####################################################################
