##########################################################################################
#
# Templated Sensors
#
##########################################################################################

# "spy mode" for light automation in l2storage
# if on the automation for the light is off
# template:
- binary_sensor:
    - default_entity_id: binary_sensor.l2storage_spymode
      name: SpyMode
      state: '{{ states("automation.l2_storage_motion_detected_ssr01_on") != ''on'' }}'
# was:
#binary_sensor:

# "spy mode" for light automation in l2storage
# if on the automation for the light is off
# - platform: template
#   sensors:
#     l2storage_spymode:
#       friendly_name: "SpyMode"
#       value_template: >-
#         {{ states("automation.l2_storage_motion_detected_ssr01_on") != 'on' }}

# ############################################################
#  END OF FILE
# ############################################################

