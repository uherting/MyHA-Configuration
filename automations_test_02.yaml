# Just a test from the GUI enhanced with the editor
- id: "1656941086689"
  alias: Test 0815
  description: Dies is ein Test
  trigger:
    - platform: state
      entity_id:
        - binary_sensor.sensor01
      to: "on"

  # condition:
  #   condition: or
  #   conditions:
  #     - condition: state
  #       entity_id: binary_sensor.sensor02
  #       state: on
  #     - condition: and
  #       conditions:
  #         - condition: state
  #           entity_id: binary_sensor.sensor02
  #           state: "off"
  #         - condition: not
  #           conditions:
  #             - condition: state
  #               entity_id: binary_sensor.sensor02
  #               state: "off"
  #               for: "00:01:01"
  condition:
    condition: template
    value_template: "{{
      is_state('binary_sensor.sensor02', 'on')
      or
      (
      is_state('binary_sensor.sensor02', 'off') and
      (now() - states.binary_sensor.sensor02.last_changed).seconds < 60
      )
      }}"
  # explanation
  # condition: or
  # conditions:
  #   - condition: either binary_sensor.sensor01 is on
  #     OR
  #   - condition: and
  #     conditions:
  #       - condition: binary_sensor.sensor01 is off
  #       AND
  #       - condition: not
  # for NOT MORE than "00:01:01" minutes === LESS THAN
  #         conditions:
  #           - condition: state
  #             entity_id: binary_sensor.sensor01
  #             state: "off"
  #             for: "00:01:01"

  action:
    - type: turn_off
      device_id: 9d7c6a3ee6ec3caa06547fbf99671661
      entity_id: switch.l2_storage01_ssr_01
      domain: switch
  mode: single
